<h1>Chowsource Contributors:</h1>

<%= will_paginate @users %>
<ul>
  <% if @users %>
    <% @users.each do |user| %>
      <li><%= link_to user.name, user %></li>
    <% end %>
  <% end %>
</ul>

<%= will_paginate @users %>
